#include "./scope.h"

namespace tvm {
namespace ir {

TVM_REGISTER_NODE_TYPE(CallbackNode);
TVM_REGISTER_NODE_TYPE(BaseScopeNode);
TVM_REGISTER_NODE_TYPE(ScopeManagerNode);
TVM_REGISTER_NODE_TYPE(IRModuleScopeNode);

}  // namespace ir
}  // namespace tvm
